[project]
name = "my-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "mlx-code>=0.0.1",
    "pydantic>=2.13.4",
    "sentence-transformers>=5.5.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
